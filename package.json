{
  "name": "withjayant",
  "version": "1.0.0",
  "description": "WithJayant - Blockchain Social Media",
  "scripts": {
    "chain": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}
